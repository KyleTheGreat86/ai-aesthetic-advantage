
import React, { memo } from 'react';

const FloatingShapes = () => {
  // Component is now empty - no more floating shapes
  return null;
};

// Use memo to prevent unnecessary re-renders
export default memo(FloatingShapes);
